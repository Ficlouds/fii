'use client';

import { createListLocalFilesInspector } from '@ficlouds/shared-tool-ui/inspectors';

export const ListLocalFilesInspector = createListLocalFilesInspector(
  'builtins.lobe-local-system.apiName.listLocalFiles',
);
