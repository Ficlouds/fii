'use client';

import { createGlobLocalFilesInspector } from '@ficlouds/shared-tool-ui/inspectors';

export const GlobLocalFilesInspector = createGlobLocalFilesInspector(
  'builtins.lobe-cloud-sandbox.apiName.globLocalFiles',
);
